{
  "name": "bams-blockchain-attendance-system",
  "version": "1.0.0",
  "description": "A multi-layered Blockchain-Based Attendance Management System (BAMS) using Node.js.",
  "main": "backend/index.js",
  "scripts": {
    "start": "node backend/index.js",
    "dev": "nodemon backend/index.js",
    "build": "npm run copy-data && cd frontend && npm install && npm run build",
    "vercel-build": "npm install && npm run copy-data && cd frontend && npm install && npm run build",
    "copy-data": "mkdir -p /tmp/backend/data && cp -r backend/data/* /tmp/backend/data/ || true"
  },
  "keywords": [
    "blockchain",
    "nodejs",
    "express",
    "attendance",
    "sha256",
    "pow"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}